import React, { useEffect } from "react";
import { useParams } from "react-router-dom";
const Redirect = () => {
  let { id } = useParams();
  useEffect(() => {
      effect
      return () => {
          cleanup
      }
  }, [input])
  console.log(id);
  return (
    <>
      <p>{id}</p>
    </>
  );
};
export default Redirect;
